import { useParams } from "react-router-dom";

const Offer = (props) => {
  // console.log(props);
  console.log(useParams());
  return <div className="offer-details"></div>;
};
export default Offer;
